country_decisions = {
	start_war_process = {
		major = yes
		potential = {
			tag = MNG
			NOT = { has_country_flag = chingchongnipnong }
			TPG = { is_subject_of = MNG }
		}
		allow = { is_year = 1490 }
		effect = {
			set_country_flag = chingchongnipnong
			TPG = { country_event = { id = ming_taiping_war.1 } }
			add_prestige = 10
		}
	}
}